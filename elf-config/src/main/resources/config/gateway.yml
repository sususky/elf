spring:
  cloud:
    gateway:
      routes:
      - id: path_route
        uri: http://localhost:8082/log
        predicates:
        - Path=/log
      - id: auth_route
        uri: lb://auth:8081
        predicates:
        - Path=/auth/{segment}


